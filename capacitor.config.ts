import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'copy-of-copy-of-gradetracker-pro',
  webDir: 'dist'
};

export default config;
